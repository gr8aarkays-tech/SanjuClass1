services:
  - type: web
    name: schoolbuddy-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    plan: free

    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
