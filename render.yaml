services:
  - type: web
    name: schoolbuddy-backend
    runtime: node
    rootDir: backend
    buildCommand: npm ci
    startCommand: node src/index.js
    plan: free
    nodeVersion: 20.x

    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production